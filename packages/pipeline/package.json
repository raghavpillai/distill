{
  "name": "@ccc/pipeline",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "pipeline": "bun run src/extract.ts && bun run src/embed.ts && bun run src/cluster.ts && bun run src/session_cluster.ts && bun run src/label.ts && bun run src/suggest.ts && bun run src/export.ts",
    "extract": "bun run src/extract.ts",
    "embed": "bun run src/embed.ts",
    "cluster": "bun run src/cluster.ts",
    "session_cluster": "bun run src/session_cluster.ts",
    "label": "bun run src/label.ts",
    "suggest": "bun run src/suggest.ts",
    "export": "bun run src/export.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@ai-sdk/anthropic": "^3.0.71",
    "@ai-sdk/openai": "^3.0.53",
    "ai": "^6.0.168",
    "hdbscan-ts": "^1.0.16",
    "ollama-ai-provider-v2": "^3.5.0",
    "umap-js": "^1.4.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@ai-sdk/provider": "^3.0.8",
    "@types/bun": "^1.3.0",
    "@types/node": "^25.6.0",
    "typescript": "^5.7.0"
  }
}
